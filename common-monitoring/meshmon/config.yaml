---
apiVersion: v1
kind: ConfigMap
metadata:
  name: meshmon-config
  namespace: meshmon
data:
  nodeconf.yml: |
    networks:
      - directory: homelads
        node_id: jens-${CLUSTER_NAME}
        config_type: git
        git_repo: https://github.com/rippleFCL/distmon-test.git

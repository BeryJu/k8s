apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: remote-write
  namespace: monitoring-system
spec:
  path: kv/services/prometheus-pub
  type: Opaque
  templates:
    username: "{% .Secrets.user %}"
    password: "{% .Secrets.password %}"

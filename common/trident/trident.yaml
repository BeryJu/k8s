apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: trident
  namespace: trident
spec:
  interval: 6h
  releaseName: trident
  chart:
    spec:
      # renovate: registryUrl=https://netapp.github.io/trident-helm-chart
      chart: trident-operator
      version: 21.10.1
      sourceRef:
        kind: HelmRepository
        name: netapp-trident
        namespace: flux-system

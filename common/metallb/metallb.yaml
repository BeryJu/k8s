apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: metallb
  namespace: metallb-system
spec:
  maxHistory: 3
  interval: 10m0s
  releaseName: metallb
  chart:
    spec:
      chart: metallb
      version: 2.3.2
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system

apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: discord-url
  namespace: flux-system
spec:
  keys:
    - discord_webhook
  path: kv/k8s-flux
  type: Opaque
  templates:
    address: "{% .Secrets.discord_webhook %}"

apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: graylog-http-username
  namespace: logging
spec:
  path: kv/services/log-pub
  type: Opaque
  templates:
    GRAYLOG_HTTP_USERNAME: "{% .Secrets.username %}"
    GRAYLOG_HTTP_PASSWORD: "{% .Secrets.password %}"

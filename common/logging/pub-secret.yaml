apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: loki-http-username
  namespace: logging
spec:
  path: kv/services/log-pub
  type: Opaque
  templates:
    LOKI_HTTP_USERNAME: "{% .Secrets.username %}"
    LOKI_HTTP_PASSWORD: "{% .Secrets.password %}"

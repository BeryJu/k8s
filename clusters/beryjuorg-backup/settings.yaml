---
apiVersion: v1
kind: ConfigMap
metadata:
  namespace: flux-system
  name: cluster-settings
data:
  # Vault
  VAULT_AUTH_PATH: "auth/k8s-backup"
  # Flux Notifications
  FLUX_SENTRY_ENV: "beryjuorg-backup"

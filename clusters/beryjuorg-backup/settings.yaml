---
apiVersion: v1
kind: ConfigMap
metadata:
  namespace: flux-system
  name: cluster-settings
data:
  # Vault
  VAULT_AUTH_PATH: "auth/k8s-backup"
  # Cluster name
  CLUSTER_NAME: "beryjuorg-backup"
  # Ingress public IP
  INGRESS_IP: "51.15.187.81"

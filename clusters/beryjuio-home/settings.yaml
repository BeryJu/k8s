---
apiVersion: v1
kind: ConfigMap
metadata:
  namespace: flux-system
  name: cluster-settings
data:
  # Vault
  VAULT_AUTH_PATH: "auth/k8s-beryjuio-home"
  # Cluster name
  CLUSTER_NAME: "beryjuio-home"
  CLUSTER_NETWORK: onpr-beryjuio-home
  CLUSTER_LOCAL_NETWORKS: "10.120.20.0/24"

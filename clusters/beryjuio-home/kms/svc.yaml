apiVersion: v1
kind: Service
metadata:
  name: vlmcsd
  namespace: kms
spec:
  ports:
    - name: kms
      port: 1688
      protocol: TCP
      targetPort: kms
  selector:
    app.kubernetes.io/instance: vlmcsd
    app.kubernetes.io/name: vlmcsd
  type: LoadBalancer

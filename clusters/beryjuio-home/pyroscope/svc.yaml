apiVersion: v1
kind: Service
metadata:
  name: pyroscope
  namespace: pyroscope
  labels:
    app.kubernetes.io/name: pyroscope
spec:
  ports:
    - port: 4040
      name: http
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/name: pyroscope
  type: ClusterIP

apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: phlare-agent
  namespace: phlare
spec:
  endpoints:
    - port: http
  selector:
    matchLabels:
      app.kubernetes.io/name: phlare

apiVersion: v1
kind: Service
metadata:
  name: phlare
  namespace: phlare
  labels:
    app.kubernetes.io/name: phlare
spec:
  ports:
    - port: 80
      name: http
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/name: phlare
  type: ClusterIP

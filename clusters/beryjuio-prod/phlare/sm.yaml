apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: phlare
  namespace: phlare
spec:
  endpoints:
    - port: http-metrics
  selector:
    matchLabels:
      app.kubernetes.io/name: phlare

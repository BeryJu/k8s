apiVersion: v1
kind: Service
metadata:
  name: kopia
  namespace: kopia
  labels:
    app.kubernetes.io/name: kopia
spec:
  ports:
    - port: 51515
      name: http
      protocol: TCP
      targetPort: http
    - port: 9300
      name: http-metrics
      protocol: TCP
      targetPort: http-metrics
  selector:
    app.kubernetes.io/name: kopia
  type: ClusterIP

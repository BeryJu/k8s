apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: workstation-data
  namespace: workstation
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: ontap-ssd

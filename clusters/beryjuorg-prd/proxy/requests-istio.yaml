---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: beryjuorg-global-overseerr
  namespace: proxy
spec:
  hosts:
  - overseerr.defr.beryju
  http:
  - route:
    - destination:
        host: overseerr.overseerr.svc.cluster.local
---
apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  name: beryjuorg-global-overseerr
  namespace: proxy
spec:
  hosts:
  - overseerr.defr.beryju
  ports:
  - number: 5055
    name: http
    protocol: HTTP
  location: MESH_INTERNAL
  resolution: DNS

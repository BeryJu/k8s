apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: beryjuorg-prometheus-artifactory-exporter-secrets
  namespace: artifactory
spec:
  path: kv/services/artifactory
  type: Opaque
  templates:
    values.yaml: |
      artifactory:
        accessToken: "{% .Secrets.api_key %}"

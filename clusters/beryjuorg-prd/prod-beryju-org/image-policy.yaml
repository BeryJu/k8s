apiVersion: image.toolkit.fluxcd.io/v1alpha2
kind: ImagePolicy
metadata:
  name: beryjuorg
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: beryjuorg-ghcr
  filterTags:
    pattern: '^auto-(?P<ts>.*)'
    extract: '$ts'
  policy:
    numerical:
      order: asc

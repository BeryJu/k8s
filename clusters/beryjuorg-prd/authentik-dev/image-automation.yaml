apiVersion: image.toolkit.fluxcd.io/v1alpha2
kind: ImageUpdateAutomation
metadata:
  name: authentik-dev-update
  namespace: flux-system
spec:
  sourceRef:
    kind: GitRepository
    name: flux-system
  interval: 5m
  update:
    path: ./clusters/beryjuorg-prd/authentik-dev
    strategy: Setters
  git:
    checkout:
      ref:
        branch: main
    commit:
      author:
        name: fluxbot[clusters/beryjuorg-prd/authentik-dev]
        email: admin@beryju.org
      messageTemplate: |
        clusters/beryjuorg-prd/authentik-dev: bump authentik dev

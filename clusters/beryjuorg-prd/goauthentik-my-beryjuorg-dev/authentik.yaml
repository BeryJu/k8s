apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: authentik
  namespace: goauthentik-my-beryjuorg-dev
spec:
  chart:
    spec:
      # renovate: registryUrl=https://charts.goauthentik.io/
      chart: authentik
      version: 4.0.2
      sourceRef:
        kind: HelmRepository
        name: authentik
        namespace: flux-system
  timeout: 10m0s
  interval: 1h0m0s
  releaseName: authentik
  valuesFrom:
    - kind: Secret
      name: authentik-secrets
  values:
    replicas: 2
    serviceAccount:
      create: false
    ingress:
      enabled: true
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
      hosts:
        - host: beryjuorg-dev.my.goauthentik.io
          paths:
            - path: "/"
              pathType: Prefix
      tls:
        - hosts: [beryjuorg-dev.my.goauthentik.io]
          secretName: authentik-acme
    postgresql:
      enabled: true
      metrics:
        enabled: true
        serviceMonitor:
          enabled: true
    redis:
      enabled: true
      architecture: standalone
      metrics:
        enabled: true
        serviceMonitor:
          enabled: true
    fullnameOverride: authentik
    image:
      repository: ghcr.io/goauthentik/dev-server
      tag: gh-master-1637141339-75f850f4d2165487d6e1e7670662837beec14d7a # {"$imagepolicy": "flux-system:authentik-gh-master:tag"}
      pullPolicy: Always
    prometheus:
      serviceMonitor:
        create: true
    geoip:
      enabled: true
    authentik:
      postgresql:
        name: "authentik"
        user: "authentik"
      log_level: debug
      email:
        from: authentik@services.beryju.org
        host: email-smtp.eu-central-1.amazonaws.com
        port: 587
        use_tls: true
      outposts:
        docker_image_base: ghcr.io/goauthentik/dev-%(type)s:gh-%(build_hash)s
      error_reporting:
        enabled: true
        environment: beryjuorg-dev
        send_pii: true

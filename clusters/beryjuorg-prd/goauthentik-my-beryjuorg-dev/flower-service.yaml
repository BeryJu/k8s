apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: flower
  name: flower-web
  namespace: goauthentik-my-beryjuorg-dev
spec:
  ports:
    - name: http
      port: 5555
      protocol: TCP
      targetPort: 5555
  selector:
    app.kubernetes.io/name: flower
  type: ClusterIP

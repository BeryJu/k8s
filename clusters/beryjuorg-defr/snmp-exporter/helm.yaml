apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: snmp-exporter
  namespace: snmp-exporter
spec:
  timeout: 30m
  interval: 6h
  chart:
    spec:
      chart: prometheus-snmp-exporter
      version: 1.2.0
      sourceRef:
        kind: HelmRepository
        name: prometheus-community
        namespace: flux-system
  values:
    serviceMonitor:
      enabled: true
      namespace: snmp-exporter
      params:
        - name: esxi3.prod.beryju.org
          module:
            - if_mib
          target: esxi3ipmi.prod.beryju.org
          interval: 1m
          scrapeTimeout: 1m
        - name: esxi4.prod.beryju.org
          module:
            - if_mib
          target: esxi4ipmi.prod.beryju.org
          interval: 1m
          scrapeTimeout: 1m
        - name: switch-tv.net.beryju.org
          module:
            - if_mib
          target: switch-tv.net.beryju.org
          interval: 1m
          scrapeTimeout: 1m
        - name: switch-kitchen.net.beryju.org
          module:
            - if_mib
          target: switch-kitchen.net.beryju.org
          interval: 1m
          scrapeTimeout: 1m

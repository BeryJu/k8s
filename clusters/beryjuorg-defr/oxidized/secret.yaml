apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: beryjuorg-oxidized
  namespace: oxidized
spec:
  path: kv/services/oxidized
  type: Opaque
  templates:
    id_rsa: "{% .Secrets.private_key %}"
    id_rsa.pub: "{% .Secrets.public_key %}"

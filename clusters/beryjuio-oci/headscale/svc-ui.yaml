apiVersion: v1
kind: Service
metadata:
  name: headscale-ui
  namespace: headscale
spec:
  selector:
    app.kubernetes.io/name: headscale-ui
  ports:
    - port: 80
      name: http
      targetPort: http

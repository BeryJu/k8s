apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: headscale-ui-settings
  namespace: headscale
spec:
  path: kv/services/headscale
  type: Opaque
  templates:
    KEY: "{% .Secrets.ui_key %}"

apiVersion: v1
kind: Service
metadata:
  name: beryjuorg-thanos-ruler-lb
  namespace: main-monitoring
spec:
  ports:
  - name: web
    port: 9092
    protocol: TCP
    targetPort: web
  selector:
    app.kubernetes.io/name: thanos-ruler
  type: LoadBalancer

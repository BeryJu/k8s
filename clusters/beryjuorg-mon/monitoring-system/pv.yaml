apiVersion: v1
kind: PersistentVolume
metadata:
  name: prom-data
  namespace: monitoring-system
  labels:
    app.kubernetes.io/name: prom-local
spec:
  storageClassName: manual
  capacity:
    storage: 100Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/srv/prometheus-data/"

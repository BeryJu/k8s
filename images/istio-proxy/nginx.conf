server {
    listen       8888;
    server_name  _;

    location / {
        proxy_pass ${INT_HOST};
        proxy_ssl_verify off;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        ${EXTRA_CONFIG};
    }
}

---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kyverno
  namespace: kyverno
spec:
  interval: 15m
  chart:
    spec:
      chart: kyverno
      version: 3.7.1
      sourceRef:
        kind: HelmRepository
        name: kyverno-charts
        namespace: flux-system
  install:
    remediation:
      retries: 5
  upgrade:
    remediation:
      retries: 5
  values:
    features:
      logging:
        format: json
    admissionController:
      serviceMonitor:
        enabled: true
      resources:
        requests: {}
    backgroundController:
      serviceMonitor:
        enabled: true
      resources:
        requests: {}
    cleanupController:
      serviceMonitor:
        enabled: true
      resources:
        requests: {}
    reportsController:
      serviceMonitor:
        enabled: true
      resources:
        requests: {}
